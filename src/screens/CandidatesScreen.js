import React from "react";
import { View, Text } from "react-native";

const CandidatesScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>CandidatesScreen</Text>
    </View>
  );
};

export default CandidatesScreen;
