import { StyleSheet, Text, View } from "react-native";

const CreateInputFields = ({ inputFields, setFieldValue }) => {
  return (
    <View>
      <Text>Input Fields</Text>
    </View>
  );
};

export default CreateInputFields;

const styles = StyleSheet.create({});
